# Panic Predictor

It's a panic predictor

## how to run

1. install dependencies：

   ```
   pip install -r requirements.txt
   ```

2. run program：

   ```
   python server.py
   ```

   web: http://localhost:5000/ 
